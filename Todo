 - elizabethae core
  -  when require plugin(or method) not found, become error.
  -  when there are same filter name, become error.
 - TopologicalSort
  -  behavior when loop.
 - Write unit test code.
 - Modefy dispatche.
 - Imprement Model and View
 - Write document.
 - Create Demo App (wiki?)
   - rich wiki??
 - View must support layout.

 - stack filter
   like application_before_filter, common_before_filter
 - support content_type

    /test/action.json
    /test/action.xml
